{
  "id": "ki_self_20260922_02",
  "title": "IoC Dispose Stack Inverse Accumulator Synchronization & Lifecycle Epoch Caching",
  "category": "kernel_and_ioc",
  "created_at": "2026-09-22T18:20:00Z",
  "author": "Brain Harness Autobiographical Reflection Engine (harness-reflector & harness-compass)",
  "tags": [
    "kernel_and_ioc",
    "service_context",
    "dispose_stack",
    "lifecycle",
    "epoch_cache",
    "endogenous_memory",
    "self_reflection"
  ],
  "context": "Dynamic service registration in micro-kernels appends inverse teardown closures to a LIFO stack. Revoking services without purging matching closures causes memory leaks and invalid invocations on container disposal. Repeated DAG sorts during plugin cascading transitions cause high compute overhead.",
  "distilled_learning": "Inverse effect closures in _dispose_stack must carry realm key and provider metadata. Targeted service revocations (revoke()) and plugin unloads (revoke_all_from()) must actively purge matching inverse closures from _dispose_stack. PluginLifecycle must cache dependency graph builds across immutable lifecycle epochs.",
  "isnad": {
    "decision_id": "dec_20260922_self_02",
    "status": "VERIFIED",
    "claims": [
      {
        "assertion": "Untracked inverse effect closures accumulate in _dispose_stack across service overrides and revocations, triggering dead effect invocations upon container disposal.",
        "lineage": [
          {
            "node_type": "primary_code",
            "uri": "src/harness/kernel/context.py#L125-L175",
            "verified": true
          },
          {
            "node_type": "test_contract",
            "uri": "tests/test_architecture_hardening.py#L20-L50",
            "verified": true
          }
        ]
      },
      {
        "assertion": "Kahn's topological sort dependency graphs are invariant within lifecycle epochs and can be memoized to accelerate multi-plugin cascading operations.",
        "lineage": [
          {
            "node_type": "primary_code",
            "uri": "src/harness/kernel/lifecycle.py#L110-L160",
            "verified": true
          },
          {
            "node_type": "rule_invariant",
            "uri": "AGENTS.md#Rule-54",
            "verified": true
          }
        ]
      }
    ]
  }
}