# FTS5 SQLite WAL Session Persistence & Compression Forking

**ID:** `ki_hermes_002`  
**Category:** `memory_and_epistemics`  
**Origin:** `NousResearch/hermes-agent`  
**Source Provenance:** `hermes_state.py#L30-L150`, `agent/context_compressor.py`

## Executive Summary
High-speed multi-platform conversation storage using SQLite WAL mode with FTS5 virtual tables, compression-triggered DAG session splitting, and cross-session semantic search.

## Architectural Invariants
1. All extracted tool patterns must map to deterministic parameter schemas.
2. Verified provenance links must cite exact source file locations.
3. State modifications must integrate into the IoC container via typed `ServiceKey[T]`.
