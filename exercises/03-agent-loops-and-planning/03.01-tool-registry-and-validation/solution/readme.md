# Solution: Register and Invoke a Tool with Validation

## Explanation

The solution registers a tool with a parameter schema and invokes it through `ToolRegistry.invoke()`, which validates arguments, formats outputs, and logs telemetry.
