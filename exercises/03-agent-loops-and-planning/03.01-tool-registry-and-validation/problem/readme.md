# Problem: Register and Invoke a Tool with Validation

## Objective

Set up a `ToolRegistry`, register a string concatenation tool with a parameter schema, and invoke it via `invoke()`.

## Tasks

1. Register `"text.concat"` expecting `str1` and `str2`.
2. Invoke the tool with parameters.
3. Handle missing parameters gracefully.
