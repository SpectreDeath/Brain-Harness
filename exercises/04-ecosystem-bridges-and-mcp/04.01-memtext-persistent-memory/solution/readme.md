# Solution: Store and Recall Memory via Memtext Bridge

## Explanation

The solution loads `MemtextServicePlugin` into `ServiceContext`, mounts memory tools into `ToolRegistry`, stores preferences with `mem.remember()`, and queries them with `mem.recall()`.
