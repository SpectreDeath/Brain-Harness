# Solution: Verify Logic Rules with Em-Cubed Bridge

## Explanation

The solution loads `EmCubedPlugin`, passes facts, rules, and goal queries into `em3.verify_logic_query()`, executing backward-chaining SLD resolution.
